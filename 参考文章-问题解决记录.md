# 学习RPG项目 · 问题解决记录

## 第一阶段：Launch 按钮修复

### 问题
- Launch 按钮点击后页面无法正常跳转或显示内容
- 提示 `window._launchApp is not defined` 错误

### 根因
1. 原始代码中 `window._launchApp` 被包裹在 IIFE 内部，暴露不明确
2. 文件编码问题导致中文乱码
3. 缺少 `loadAllData()` 和 `initTheme()` 函数定义

### 解决
1. `window._launchApp` 放在 app.js 最前面直接暴露
2. 添加 `loadAllData()` 串联加载所有数据源
3. 添加 `initTheme()` 初始化主题
4. HTML 中移除预填内容，避免解析误报

## 第二阶段：导航和其他页面无响应

### 问题
- 点击 LAUNCH 后，技能树、复习、日志页面的导航按钮完全没有反应
- 只有概览页和设置页（inline onclick）能工作

### 根因：`initNavigation()` 被放在 `loadAllData().then()` 中
```javascript
// ❌ 错误：如果 loadAllData 出错，initNavigation 永远不会被调用
loadAllData().then(function() {
  initNavigation();  // 只在数据加载成功后才绑定事件
  ...
}).catch(function(e) {
  console.error(e);  // 没有任何初始化
});
```

### 解决：把 `initNavigation()` 移到 Promise 外面
```javascript
// ✅ 正确：无论数据加载是否成功，导航事件都会被绑定
initNavigation();  // 立即绑定导航事件
loadAllData()
  .then(...)
  .catch(function(e) {
    console.error(e);
    renderAll();
  });
```

### 影响范围
- 修改文件：`js/app.js` 第37-50行

## 第三阶段：GitHub 数据拉取 + 本地数据加载

### 问题
- 在 file:// 协议下打开 index.html 时，`loadFromMultiSource()` 直接 reject
- 所有数据（data.json、skill-tree.json 等）都只能从 localStorage 读取
- GitHub 数据完全没有被拉取

### 根因
```javascript
// ❌ data.js 旧代码
function loadFromMultiSource(path) {
  if (isFileProtocol()) {
    return Promise.reject(new Error('File protocol, skip network'));
  }
  // ... never reached on file://
}
```

### 解决：file:// 下先尝试本地 XHR 加载，再 fallback 到 GitHub
```javascript
// ✅ data.js 新代码
function loadFromMultiSource(path) {
  if (isFileProtocol()) {
    return loadLocalFile(path).catch(function () {
      return tryGitHubUrls(path);  // 本地没有则尝试网络
    });
  }
  return tryGitHubUrls(path);
}

// 新增 loadLocalFile() — 用 XHR 加载本地 data/ 目录下的 JSON 文件
function loadLocalFile(path) {
  return new Promise(function (resolve, reject) {
    var xhr = new XMLHttpRequest();
    xhr.open('GET', path, true);
    xhr.timeout = 5000;
    xhr.onload = function () {
      if (xhr.status >= 200 && xhr.status < 300) {
        resolve(JSON.parse(xhr.responseText));
      } else { reject(new Error('Local file not found: ' + path)); }
    };
    xhr.onerror = function () { reject(new Error('XHR error: ' + path)); };
    xhr.send();
  });
}
```

### 额外修复：`convertGitHubData()` 自动计算 totalXp
- 旧的 `data.json` 格式没有 `profile.totalXp` 字段
- 新增代码：如果 profile 没有 totalXp，就从 records 数组中累加

### 影响范围
- 修改文件：`js/data.js`

## 成功经验总结

### 架构原则
1. **全局函数要明确暴露**：不要深度包裹在 IIFE 内部
2. **事件绑定要独立于数据加载**：initNavigation() 不能依赖异步数据
3. **file:// 协议也要能工作**：通过 XHR 加载本地 JSON 文件
4. **数据加载要有多级 fallback**：本地文件 → GitHub → localStorage → 默认值

### 文件加载顺序
```
index.html
  ├── css/theme.css       → CSS 变量（Material Design 3）
  ├── css/layout.css      → 布局/导航/响应式
  ├── css/components.css  → 按钮/卡片/输入/进度条
  ├── css/pages.css       → 5个页面布局骨架
  ├── echarts CDN         → ECharts 图表库
  ├── js/utils.js         → 工具函数 + 全局常量
  ├── js/algorithms.js    → 核心算法引擎
  ├── js/data.js          → 数据加载/缓存/持久化
  └── js/app.js           → 应用主逻辑（全部功能）
```

### 功能清单

| 模块 | 入口 | 状态 |
|------|------|------|
| Launch 启动 | 启动页 LAUNCH 按钮 | ✅ |
| 概览页 | 导航栏·总览 | ✅ |
| 概览折叠区 | 概览页下方折叠面板 | ✅ |
| 数据录入 | 日志页按钮 / 设置页 / Ctrl+K | ✅ |
| 日志页 | 导航栏·日志 | ✅ |
| 日志筛选/分页 | 日志页顶部下拉 | ✅ |
| 日志编辑/删除 | 每条记录的操作按钮 | ✅ |
| 技能树 | 导航栏·技能树 | ✅ |
| 复习中心 | 导航栏·复习 | ✅ |
| 设置页 | 导航栏·更多 | ✅ |
| 阅读记录 | 设置页→阅读记录 | ✅ |
| 番茄钟 | 右下角 FAB 按钮 | ✅ |
| 番茄钟历史 | 设置页→番茄钟历史 | ✅ |
| 命令面板 | Ctrl+K / 搜索图标 | ✅ |
| 成就系统 | 自动检测 + Toast 通知 | ✅ |
| 主题切换 | 设置页 / 命令面板 | ✅ |
| 数据导出 | 设置页→导出数据 | ✅ |

## 第四阶段：XP算法优化 + localStorage 优先策略

### XP引擎升级

**问题：** 保存学习记录时用 `calcBaseXP(score, duration)` 简单公式，不涉及学科难度、动量、活动权重等完整加权模型。

**解决：** 新增 `js/recorder.js`，所有记录创建通过 `createLearningRecord()` → 内部调用 `calcXPEngine2()` 完整加权公式 → `commitRecord()` 自动持久化。

`calcXPEngine2` 因子链：
```
XP = rawXP × qual × decay × softCap
rawXP = 做题时长×PE + 订正时长×CE×1.3
qual = E × 学科难度 × (1+动量) × 活动权重
E = PE×(1-订正比) + CE×订正比×1.3
decay = 1/(1+总XP×衰减率)
softCap = 日软上限打折
```

### localStorage 优先策略

**问题：** 旧 `loadAppData()` 是先查24h缓存 → GitHub网络请求 → 最后才 localStorage。网络失败或慢时整个加载阻塞。

**解决：** 新增 `js/persistence.js`，加载顺序改为：
```
localStorage（即时）→ 缓存 → 网络 → 默认值
```
启动后2秒后台静默同步 GitHub，合并远程新记录到本地。

### 文件隔离设计

| 文件 | 类型 | 说明 |
|------|------|------|
| `js/persistence.js` | 新建 | localStorage 优先 + 后台同步 + 合并逻辑 |
| `js/recorder.js` | 新建 | `createLearningRecord` / `commitRecord` / `removeRecordById` / `updateRecordById` |
| `js/data.js` | 编辑 | `loadAppData` 改为 localStorage 优先；`backgroundDataLoad` 增加日志 |
| `js/app.js` | 编辑 | `_saveRecord`/`_saveEdit`/`_confirmDelete`/番茄钟 → 全部改用 recorder 函数 |
| `index.html` | 编辑 | 加载新 JS 文件（persistence.js + recorder.js） |
| `css/layout.css` | 无改动 | 上一轮番茄钟 CSS 不变 |

### 影响范围总结

- **算法层**（recorder.js）：所有数据写入统一入口，XP 使用完整加权模型
- **持久层**（persistence.js）：启动秒级加载，不再依赖网络
- **应用层**（app.js）：`_saveRecord` 代码量减少 60%，删除 ~50 行重复逻辑
- **隔离性**：core 修改仅限 4 个调用点，CSS/utils/algorithms 完全未触碰

